# Conventions

- WooCommerce payment gateway. PHP 8.2+. Independent of the WordPress plugin repo.
- Server: official PHP SDK. Checkout JS: official JS/TS SDK bundled into `assets/js/checkout.js`.
- REST paths match the JS/TS `DEFAULT_PATHS` convention. Never localize merchant API keys.
